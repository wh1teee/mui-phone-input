# @whiteee/mui-phone-input

Implementation workspace for the future MUI Phone Input package.

The package currently exposes only empty, production-shaped entrypoints. Public
phone-input runtime behavior remains blocked on the real-browser Input
Transaction engine bake-off tracked by `mpi-oan.22`.

