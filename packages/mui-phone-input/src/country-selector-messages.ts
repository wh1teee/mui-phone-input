/** UI-independent messages shared by renderer adapters and locale packs. */
export interface PhoneCountrySelectorMessages {
  allCountries: string;
  close: string;
  dialogTitle: string;
  noOptions: string;
  preferredCountries: string;
  searchLabel: string;
  selectCountry: string;
}
