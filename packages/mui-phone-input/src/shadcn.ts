'use client';

// One Base UI composition. Import /shadcn.css explicitly, or provide owned slot
// classes. No implicit stylesheet, Tailwind runtime or alternate editing state.
export * from './base-ui';
