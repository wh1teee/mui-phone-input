---
'@wh1teee/mui-phone-input': patch
---

Declare `@types/react` as an optional peer so isolated pnpm Global Virtual Store consumers resolve the published declarations without inheriting workspace-only type packages. Runtime exports and bundle bytes are unchanged.
